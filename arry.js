var firendsAge = [11,12,13,14]; //array declare

var sonaliAge = firendsAge[2]; // kno element ber kore niye asa


firendsAge[1] = 21;  // kno element add korar program

var position = firendsAge.indexOf(13); // position ber korar program

firendsAge.push(15); 
                    // array ar moddhe element push korar niyom
firendsAge.push(17);


// console.log(firendsAge.length) // lenght ber korar niyom

//push and pop element at the begging
var firendsAge = [11,12,13,14];
//firendsAge.unshift(100);
//console.log(firendsAge)

// pop element at the begging
 firendsAge.shift(11);
 console.log(firendsAge)


